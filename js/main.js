import { iniciarMenu } from "../components/menu.js";

const carregarComponente = async (caminho, container) =>{
    try{

            const resposta = await feitch(caminho);

    }catch (erro){
   
    }
};

const iniciarLinks = () =>{
};

const iniciarApp =async () =>{
};

iniciarApp();


 